name: generate_signin_key
description: A command-line tool to easily generate and view Android signing keys (SHA-1, SHA-256) for your Flutter/Android projects.
version: 1.1.1
homepage: https://github.com/bluedev-in/generate_signin_key
repository: https://github.com/bluedev-in/generate_signin_key

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.5.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0

executables:
  generate_signin_key:
